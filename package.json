{
  "name": "spotify-connect",
  "version": "0.0.1",
  "description": "Spotify account linking",
  "main": "index.js",
  "scripts": {
    "start": "node -e \"require('dotenv').config(); require('.').listen(4000);\"",
    "daemon": "node -e \"require('dotenv').config(); require('./daemon')\"",
    "server": "node -e \"require('dotenv').config(); require('./server')\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/blackboxrecords/spotify-connect.git"
  },
  "author": "Chance Hudson",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/blackboxrecords/spotify-connect/issues"
  },
  "homepage": "https://github.com/blackboxrecords/spotify-connect#readme",
  "dependencies": {
    "@jchancehud/parallel-promise": "0.0.2",
    "axios": "^0.18.0",
    "express": "^4.16.4",
    "mongoose": "^5.5.7"
  },
  "devDependencies": {
    "dotenv": "^8.0.0"
  }
}
